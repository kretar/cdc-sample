version: 2
jobs:
  build:
    machine:
      image: ubuntu-2004:current
    working_directory: ~/project
    steps:
      - checkout
      - run:
          name: Start contract repo
          command: |
            set -x
            docker-compose up -d
      - run: docker-compose run --rm builder ./gradlew build
