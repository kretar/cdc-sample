version: 2
jobs:
  build:
    machine: true
    working_directory: ~/project
    steps:
      - checkout
      - run:
          name: Start contract repo
          command: |
            set -x
            docker-compose up -d
      - run: docker-compose run --rm builder mvn clean verify
